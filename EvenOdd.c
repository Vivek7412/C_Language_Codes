main()
{
    int num;
    printf("Enter an integer:");
    scanf("%d",&num);
    if(num%2==0)
    {
        printf("no. is even",num);
    }
    else
    {
        printf("no. is odd",num);
    }
    return 0;
}
